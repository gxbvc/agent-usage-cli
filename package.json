{
  "name": "agent-usage-cli",
  "version": "1.0.0",
  "description": "Read subscription usage for locally logged-in Claude Code, Codex, and Grok Build CLIs.",
  "type": "module",
  "private": true,
  "bin": {
    "agent-usage-cli": "./bin/agent-usage-cli.js"
  },
  "files": [
    "bin/",
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
